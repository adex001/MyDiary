import TokenHandler from '../middleware/tokenhandler';

describe('Tests Token middlewares', () => {
  const req = {
    headers: {
      token: 'fbwebweibweibwebewibviwdbvd.ewfewbfjehwbeververvre.regregergerhwtrhtr',
    },
  };
});
